apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "uifactory.name" . }}-config
  labels:
    {{- include "uifactory.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
